{
  "name": "valomarssi",
  "version": "0.1.0",
  "description": "Baby's first raymarching implementation",
  "scripts": {
    "dev": "parcel src/index.html --no-cache",
    "clean": "rm -rf .parcel-cache && rm -rf dist",
    "build": "parcel build src/index.html --no-cache --no-source-maps --no-content-hash",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ilkka Hänninen",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-glsl": "^2.4.1",
    "crunchme": "^0.0.4",
    "parcel": "^2.4.1",
    "typescript": "^4.6.3"
  }
}
